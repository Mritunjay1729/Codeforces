#include <iostream>
#include <vector>

using namespace std;

int main() {
    // vector<int> fibo;
    int num;
    cin >> num;
    // fibo.push_back(0);
    // fibo.push_back(1);
    // int i=1;
    // while(fibo[i] < num) {
    //     int fib_next = fibo[i] + fibo[i-1];
    //     fibo.push_back(fib_next);
    //     i++;
    // }

    printf("%d %d %d\n", 0, 0, num);
    return 0;
}